export type Upgrades = {
  moveSpeed: number;
  maxHealth: number;
  reloadSpeed: number;
  abilityHaste: number;
  shotDamage: number;
  castPower: number; // Poder de Conjuração
  vampirism: number;
};

export const upgrades: Upgrades = {
  moveSpeed: 0,
  maxHealth: 0,
  reloadSpeed: 0,
  abilityHaste: 0,
  shotDamage: 0,
  castPower: 0,
  vampirism: 0,
};

