export type Upgrades = {
  moveSpeed: number;
  maxHealth: number;
  reloadSpeed: number;
  luck: number;
  projectileSpeed: number;
  abilityHaste: number;
  shotDamage: number;
  magnetRadius: number;
};

export const upgrades: Upgrades = {
  moveSpeed: 0,
  maxHealth: 0,
  reloadSpeed: 0,
  luck: 0,
  projectileSpeed: 0,
  abilityHaste: 0,
  shotDamage: 0,
  magnetRadius: 0,
};
